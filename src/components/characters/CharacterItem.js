import React from 'react';

// destructing props
const CharacterItem = ({ item }) => {
    // console.log(item)
    return (

            <h3>{item.name}</h3>

    )
  }
  
  export default CharacterItem
